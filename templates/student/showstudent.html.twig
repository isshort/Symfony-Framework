{% extends 'index.html.twig' %}

{% block body %}
    <div class="post-preview">
        <table border=1>
            <tr><th>ID</th><th>Name</th><th>Last Name</th><th>year</th></tr>

            {% for student in students %}
{#                <tr><td>{{ student.getId }}</td><td>{{ student.getName }}</td><td>{{ student.getLastName }}</td><td>{{ student.getYear }}</td></tr>#}
                <tr><td>{{ student['id'] }}</td><td>{{ student['name'] }}</td><td>{{ student['last_name'] }}</td><td>{{ student['year'] }}</td></tr>
            {% endfor %}
        </table>
        <br>
    </div>


{% endblock %}
